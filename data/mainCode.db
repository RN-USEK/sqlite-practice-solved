CREATE TABLE Users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    first_name TEXT NOT NULL,
    last_name TEXT NOT NULL,
    motto TEXT
);


CREATE TABLE Dogs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL
);

CREATE TABLE Favorites (
    user_id INTEGER,
    dog_id INTEGER,
    FOREIGN KEY (user_id) REFERENCES Users(id),
    FOREIGN KEY (dog_id) REFERENCES Dogs(id),
    CONSTRAINT unique_favorites UNIQUE(user_id, dog_id)
);


INSERT INTO Users (first_name, last_name, motto)
VALUES ('Jon', 'Higgz', 'I love coding'),
       ('Andrey', 'Rusterton', 'I love coding even more'),
       ('Peter', 'Garboni', 'I love coding even more');

INSERT INTO Dogs (name)
VALUES ('DOOMSLAYER'),
       ('Zoey'),
       ('Jefferey');

INSERT INTO Favorites (user_id, dog_id)
VALUES (1, 1), -- Jon Favorites DOOMSLAYER
       (2, 1), -- Andrey Favorites DOOMSLAYER
       (3, 1), -- Peter Favorites DOOMSLAYER
       (1, 3), -- Jon Favorites Jefferey
       (2, 2); -- Andrey Favorites Zoey
